# For Gemini-based agents

All rules live in **[AGENTS.md](./AGENTS.md)** — the single source of truth for every agent tool.
Read it first. This file is intentionally thin to avoid a second source of truth (pattern: each
tool gets its own conventional entrypoint file; all point at AGENTS.md — add CONVENTIONS.md etc.
the same way if another tool needs one).
