{"verdict":"REQUEST_CHANGES","findings":[{"category":"unclear_acceptance","target":"## Acceptance criteria","blocking":true,"evidence":"Bullets name a tolerant profile, all-branches, and fuzzy heuristics with no file/flag/current-vs-desired observable. packages/repo-quality/secret-scan.ts only maps dir|staged|history plus --redact (no branch or detector-profile switch). docs/QUALITY-LINT.md says Betterleaks host defaults plus that wrapper are SoT and there is no kit .betterleaks.toml.","requiredEdit":"Replace ## Acceptance criteria with: (1) Kit-owned Betterleaks policy lives in packages/repo-quality/secret-scan.ts (and regenerated secret-scan.mjs) and, if flags are insufficient, a kit-owned config file under packages/repo-quality/; that policy is the template default; docs/QUALITY-LINT.md still forbids consumer-copied .betterleaks.toml except issue-linked allowlists. (2) All-branches: delete this bullet if secret:dir and secret:staged already have no default-branch filter; otherwise name the exact repo-local file and key that currently limits scanning to the default branch and the replacement that applies on every branch (no GitHub-org UI). (3) Keep-list is exactly sk-, ghp_, AKIA, PEM blocks, and JWTs; entropy/generic heuristics are absent from that named kit policy file. (4) Isolated false-positive fixture exits 0; isolated AKIA, ghp_, and PEM fixtures exit non-zero."},{"category":"insufficient_verify_gate","target":"## Verify","blocking":true,"evidence":"Gate is only corepack pnpm verify. That runs pnpm secret:dir over . and pnpm test only packages/adoption-shell/test/*.test.ts; it does not prove keep-list vs dropped heuristics or isolated FP/TP fixtures. A committed AKIA/ghp_/PEM fixture would fail the same secret:dir. Wrapper rejects extra argv (length !== 3).","requiredEdit":"Replace the ## Verify bash fence with: corepack pnpm verify && node --test packages/repo-quality/secret-scan.policy.test.ts. Add a Changes bullet that secret-scan.policy.test.ts plants FP and TP fixtures in a temp directory (never tracked under the secret:dir tree), invokes Betterleaks/the wrapper against that directory, asserts FP exit 0 and AKIA/ghp_/PEM exit non-zero, then deletes the temp dir. If secret-scan.ts changes, run pnpm repo-quality:emit so existing pnpm repo-quality:emit:check passes. If the new test file is tracked, add template-manifest.json to - **Scope-write:**."},{"category":"missing_files_surfaces","target":"## Changes","blocking":false,"evidence":"Changes is not file-by-file. Scope-write is only packages/repo-quality; a policy change also needs CHANGELOG.md [Unreleased] per AGENTS.md and docs/QUALITY-LINT.md which currently asserts Betterleaks defaults as SoT.","requiredEdit":""},{"category":"oversized_scope","target":"# Plan NNN","blocking":false,"evidence":"H1 says fleet-wide all branches; Out of scope correctly limits this item to repo-template only (DOCTRINE §11). Title can pull an implementer across repos.","requiredEdit":""}],"confidence":"high"}
