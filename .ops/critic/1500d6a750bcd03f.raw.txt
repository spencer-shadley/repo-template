{"verdict":"REQUEST_CHANGES","findings":[{"category":"unclear_acceptance","target":"## Acceptance criteria (all-branches)","blocking":true,"evidence":"AC and Changes require all-branch application; packages/repo-quality/secret-scan.ts has no branch filter and .github/workflows is absent. Verify is only `corepack pnpm verify`, which cannot prove all refs or GitHub-org scan settings.","requiredEdit":"In ## Acceptance criteria replace `It applies on all branches, not only the default branch.` with `packages/repo-quality/secret-scan.ts dir, staged, and history command vectors have no default-branch-only filter (no on.branches, --since, or equivalent).` In ## Changes delete `Configure it to apply on all branches, not only the default branch.` unless the plan names an in-repo path and the exact filter to change. GitHub-org secret-scanning settings stay out of scope."},{"category":"insufficient_verify_gate","target":"## Verify","blocking":true,"evidence":"AC requires a known-bad AKIA/ghp_/PEM fixture to fail; package.json `verify` runs `pnpm secret:dir` on `.` and `test` only runs packages/adoption-shell/test/*.test.ts. A committed known-bad fixture would fail the authoritative gate; the Verify fence has no isolated fail-case command.","requiredEdit":"In ## Verify replace the bash fence with: `corepack pnpm verify && node --test packages/repo-quality/secret-scan.fixtures.test.ts`. In ## Changes add: `packages/repo-quality/secret-scan.fixtures.test.ts` runs Betterleaks against an isolated false-positive fixture directory (expect exit 0) and an isolated known-bad directory with AKIA, ghp_, PEM, and JWT shapes (expect non-zero); those fixtures must not be committed on the live tree that `pnpm secret:dir` scans."},{"category":"missing_files_surfaces","target":"## Changes / Scope-write","blocking":false,"evidence":"Changes is not file-by-file. Current SoT is Betterleaks vendor defaults (docs/QUALITY-LINT.md; scripts/verify-quality-lint-required.ts root .betterleaks.toml check). Scope-write is only packages/repo-quality.","requiredEdit":""},{"category":"likely_failure_mode","target":"## Changes","blocking":false,"evidence":"A repo-root .betterleaks.toml without a tracked issue URL fails verify-quality-lint-required, which still treats vendor defaults as SoT. Title/Objective say fleet-wide while Out of scope is template-only.","requiredEdit":""}],"confidence":"high"}
