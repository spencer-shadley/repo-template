{
  "name": "@repo-template/adoption-shell",
  "version": "0.0.0-private",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=24.16.0 <25"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "node tools/artifact-build.ts prepare write && tsc -p .adoption-shell-build.json && node tools/artifact-build.ts finish write",
    "build:check": "node tools/artifact-build.ts prepare check && tsc -p .adoption-shell-build.json && node tools/artifact-build.ts finish check",
    "test": "node --test packages/adoption-shell/test/*.test.ts",
    "release:payload": "node tools/release-payload.ts write",
    "release:payload:check": "node tools/release-payload.ts check",
    "verify:artifact": "node tools/artifact-build.ts verify",
    "verify:self": "node scripts/lint-user-surface-leaks.mjs --self-test && node scripts/lint-user-surface-leaks.mjs --config .user-surface-lint.json && node scripts/verify-quality-lint-required.mjs --self-test && node tools/verify-template-self.ts && node scripts/check-issue-template-intake.mjs && pnpm release:payload:check",
    "verify": "pnpm typecheck && pnpm build:check && pnpm test && pnpm verify:artifact && pnpm verify:self",
    "check:issue-template-intake": "node scripts/check-issue-template-intake.mjs",
    "verify:quality-lint": "node scripts/verify-quality-lint-required.mjs --self-test"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1",
    "typescript": "7.0.2"
  }
}
