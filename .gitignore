node_modules/
*.log
.DS_Store
.env
.env.*
*.secret.*
.orchestrator-tree.lock

# loop transient sidecars (never commit)
plans/*.log.md
plans/*.result.md
plans/*.feedback.md
plans/*.paused

# keep LAST: incident log IS tracked — guard against future blanket ignores swallowing it.
# Parent-directory ignore rules can still hide .ops before this file is read; fix those at the parent.
!.ops/
!.ops/**

# runner tree-lock mutex: transient, must never make the tree dirty (fleet-starvation incident 2026-07-02)
.orchestrator-tree.lock
