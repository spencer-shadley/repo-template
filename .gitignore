node_modules/
# runtime-artifact: owner=repo-template incident=none
.adoption-shell-build.json
*.log
.DS_Store
.env
.env.*
*.secret.*
# runtime-artifact: owner=agent-orchestrator incident=none
.orchestrator-tree.lock
# runtime-artifact: owner=agent-orchestrator incident=none
.orchestrator-*.lease
# runtime-artifact: owner=agent-orchestrator incident=none
.ops-auto-commit-state.json

# loop transient sidecars (never commit)
# runtime-artifact: owner=agent-orchestrator incident=none
plans/*.log.md
# runtime-artifact: owner=agent-orchestrator incident=none
plans/*.result.md
# runtime-artifact: owner=agent-orchestrator incident=none
plans/*.feedback.md
# runtime-artifact: owner=agent-orchestrator incident=none
plans/*.paused

# keep near LAST: incident log IS tracked; drain auto-commits sole dirty auto-appends as
# "ops: incidents (auto)" so the active log cannot wedge the queue.
# Guard against future blanket ignores swallowing it. File-precise transient re-ignores (e.g. the
# critic rule below) may still follow this block; blanket `.ops/` or `.ops/**` ignores remain forbidden.
# Parent-directory ignore rules can still hide .ops before this file is read; fix those at the parent.
!.ops/
!.ops/**
# runtime-artifact: owner=agent-orchestrator incident=none
.ops/concurrency-capture.jsonl
# runtime-artifact: owner=agent-orchestrator incident=none
.ops/freeze-expired.notify

# plan-critic/pre-enqueue writes failed verdict reports here for local diagnostics; successful
# verdicts are copied into tracked plans/<NNN>.critic.md. Re-ignore file-precise so failed runs
# don't dirty or wedge the queue (queue-abort-dirty incident).
# runtime-artifact: owner=agent-orchestrator incident=none
.ops/critic/*.md
# runtime-artifact: owner=agent-orchestrator incident=repo-template#172
.ops/issue-deps/**
# runtime-artifact: owner=agent-orchestrator incident=repo-template#172
.ops/receipts/work-spine-lag/*.json

# local tool state (Serena MCP)
.serena/

# Loop-written ephemeral receipts. These are regenerated every cycle and are NOT
# durable artifacts, but nothing ignored them, so the run-loop's own writers left
# the tree permanently dirty -- and the run-loop then refuses to start in a dirty
# tree it cannot attribute to a dead run ("Working tree is dirty and run-loop could
# not attribute it to a dead run"). File-precise, never a blanket .ops/ ignore.
# runtime-artifact: owner=agent-orchestrator incident=none
plans/*.work-spine-lag.json
# runtime-artifact: owner=agent-orchestrator incident=none
/.credit-telemetry-coalesce.json
# runtime-artifact: owner=agent-orchestrator incident=repo-template#129
.ops/work-items/QUEUE.generated.md
# runtime-artifact: owner=agent-orchestrator incident=repo-template#129
.ops/work-items/work-projection.v1.json

# runtime-artifact: owner=repo-template incident=repo-template#131
.ops/proof-of-detection-plant-ledger.json
# runtime-artifact: owner=repo-template incident=repo-template#152
.ops/jscpd-ai.txt

# runtime-artifact: owner=repo-template incident=repo-template#108
.pnpm-store/
