# Run queue — repo-template

<!-- Decision C / M9 retirement sticky: claim Pending SoT is issue/plan DAG →
     `.ops/work-items/QUEUE.generated.md` (CLAIM_SOT=1). This hand file is frozen optional
     glass (WRITE_GENERATED_QUEUE=0) — not kept fresh; do not treat ## Pending as live SoT.
     Heading toward delete after remaining readers / CLAIM_SOT=0 fallback retire. -->

RISK-TIERED repo: only docs/config `auto` plans belong in the auto-merge claim lane; executable or
cross-repository `human` plans use the one-off governed lane (`--no-queue`).

## Pending
