Changed:
- [AGENTS.md](C:/Users/Spencer/AppData/Local/agent-orchestrator/worktrees/repo-template-019/AGENTS.md:13): replaced the masked `grep && exit 1; ec=$?` gate with explicit exit-code capture so matches and grep errors fail before the Node manifest check.
- [CHANGELOG.md](C:/Users/Spencer/AppData/Local/agent-orchestrator/worktrees/repo-template-019/CHANGELOG.md:21): clarified the Unreleased entry for the `set -e`-independent grep hardening.
- Checked `docs/adr/0002-verify-gate-contract.md`; it does not duplicate the old shell pattern.

Ran:
- `bash -c 'ec=0; grep -rn "^<<<<<<<" --include="*.md" --include="*.yml" --include="*.json" --include="*.jsonl" --include="TEMPLATE_VERSION" . || ec=$?; if [ "$ec" -ne 1 ]; then exit 1; fi'` — pass
- `bash -c 'ec=0; grep -rn ZZZNOMATCH /nonexistent 2>/dev/null || ec=$?; if [ "$ec" -ne 1 ]; then echo GATE_FAILS_ON_ERROR; else echo would_pass; fi'` — pass, printed `GATE_FAILS_ON_ERROR`
- Clean isolated grep probe — pass
- Conflict-marker isolated grep probe — pass, printed `GATE_FAILS_ON_CONFLICT`
- Template manifest `node -e ...` check — pass
- `rg -n "&& exit 1; ec=\$\?|grep -rn '\^<''<<<<<<'|grep execution" AGENTS.md CHANGELOG.md docs` — pass; old masked pattern absent

Result:
- Pass. Acceptance criteria covered: clean tree passes, conflict marker fails, grep execution error fails without being masked by the subsequent Node check.

Open items:
- None.