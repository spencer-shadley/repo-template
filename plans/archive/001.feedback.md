# Verify failed - fix these before re-review

```
./plans/001.result.md:11:- `bash -lc 'cd /c/code/repo-template && ! grep -rn "<<<<<<<" --include="*.md" --include="*.yml" . && for f in README.md AGENTS.md CLAUDE.md TODO.md SECURITY.md .gitignore plans/QUEUE.md docs/ARCHITECTURE.md docs/adr/0001-design-philosophies.md docs/adr/0005-git-conventions.md TEMPLATE_VERSION CHANGELOG.md; do [ -f "$f" ] || { echo "missing $f"; exit 1; }; done && grep -q "2.2.0" TEMPLATE_VERSION && grep -q "spencer-shadley/code" README.md'`

```
