Changed:
- `AGENTS.md` — documented the wedge-free policy: keep `.ops/incidents.jsonl` tracked, and have drain auto-commit sole dirty incident-log appends as `ops: incidents (auto)`.
- `.gitignore` — updated the `.ops` tracking comment to match that policy.
- `CHANGELOG.md` — added the Unreleased entry and cited incident fingerprint `43efffab9ecedf82`.

Ran:
- `grep -n 'incidents.jsonl' AGENTS.md .gitignore template-manifest.json`
- `git diff -- AGENTS.md .gitignore CHANGELOG.md template-manifest.json`

Result:
- `grep ...`: fail. Exact error:
```text
grep: 
Line |
   2 |  … a\Local\agent-orchestrator\worktrees\repo-template-013'; grep -n 'inc …
     |                                                            ~~~~
     | The term 'grep' is not recognized as a name of a cmdlet, function, script file, or executable program.
Check the spelling of the name, or if a path was included, verify that the path is correct and try again.
```
- `git diff ...`: pass; diff showed only the three scoped doc/config changes above.

Open items:
- Per your instruction, I stopped after the command failure. The remaining quick checks still need to be run in an environment with `grep` available, or with an approved equivalent command.