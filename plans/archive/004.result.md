**Changed:**
- `docs/MIGRATION.md`: added the existing-repo overlay playbook covering case-insensitive per-file copy, swallow audit, ADR numbering, never-touch pointers, `.template-sync.json`/`skipPaths`, tiering, smoke proof, and non-Node notes.
- `README.md`: added the migration doc row and workspace-context pointer.
- `CHANGELOG.md`: added `2.1.0` entry for the new migration playbook.
- `TEMPLATE_VERSION`: bumped to `2.1.0`.
- `template-manifest.json`: added `docs/MIGRATION.md` as `copy`; also added already-tracked archived plan 003 sidecars as `self` so the repo self-gate passes.
- `plans/004.feedback.md`: deleted after addressing the reviewer feedback.

**Ran:**
- `bash -lc "! grep -rn '<''<<<<<<' --include='*.md' --include='*.yml' --include='*.json' ."`
- `bash -lc "grep -qi 'case-insensitive' docs/MIGRATION.md && grep -qi 'skipPaths' docs/MIGRATION.md"`
- `bash -lc "grep -q '2.1.0' TEMPLATE_VERSION"`
- `node -e "JSON.parse(require('fs').readFileSync('template-manifest.json','utf8'));"`
- `bash -lc "! grep -rn '<''<<<<<<' --include='*.md' --include='*.yml' --include='*.json' --include='TEMPLATE_VERSION' ."`
- `node -e "const m=require('./template-manifest.json');const allowed=new Set(['copy','merge','self','generated']);const {execSync}=require('child_process');const tracked=execSync('git ls-files',{encoding:'utf8'}).trim().split(/\r?\n/).filter(Boolean);const missing=tracked.filter(f=>!m[f]);const invalid=Object.entries(m).filter(([,v])=>!allowed.has(v)).map(([k,v])=>k+':'+v);if(missing.length||invalid.length){if(missing.length)console.error('unmanifested:',missing);if(invalid.length)console.error('invalid manifest modes:',invalid);process.exit(1)}"`
- `git diff --check`
- `node -e "const m=require('./template-manifest.json'); if (m['docs/MIGRATION.md'] !== 'copy') process.exit(1);"`

**Result:**
- All current checks passed.
- Earlier command attempts failed due cwd/quoting, then were rerun successfully. The material gate failure was: `unmanifested: [ 'plans/archive/003-v2-0-0-pre-migration-hardening-fix-the-4-blockers-audit-cons.md', 'plans/archive/003.log.md', 'plans/archive/003.result.md' ]`, fixed by manifest entries.

**Open items:**
- None. No commit or push performed.