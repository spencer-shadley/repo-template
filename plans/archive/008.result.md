Changed:
- `AGENTS.md` — updated the TEMPLATE-SELF verify gate to include `*.jsonl` in conflict-marker scans and ignore tracked `.ops/archive/` paths when calculating missing manifest entries.
- `CHANGELOG.md` — added the required Unreleased structural-change entry.

Ran:
- `grep -n "include='\*.jsonl'" AGENTS.md`
- `! grep -rn '<''<<<<<<' --include='*.md' --include='*.yml' --include='*.json' --include='*.jsonl' --include='TEMPLATE_VERSION' AGENTS.md CHANGELOG.md template-manifest.json .ops/incidents.jsonl TEMPLATE_VERSION`
- `node -e "...manifest check..."`

Result:
- Fail: `grep -n "include='\*.jsonl'" AGENTS.md`
  Exact error: `The term 'grep' is not recognized as a name of a cmdlet, function, script file, or executable program.`
- Fail: conflict-marker scan command
  Exact error: `Missing expression after unary operator '!'.`
- Fail: inline Node command as invoked from PowerShell
  Exact error: `SyntaxError: missing ) after argument list`

Open items:
- Stopped per instruction after command failures. The patch is present, but validation needs to be rerun in a shell with `grep`/bash semantics or with PowerShell-equivalent commands.