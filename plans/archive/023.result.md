Changed (files + why):
- `AGENTS.md`: TEMPLATE-SELF verify gate now scans `git ls-files -z` instead of a hardcoded extension list, with NUL-safe `xargs`, binary-as-text grep, and `--` before filenames.
- `CHANGELOG.md`: added the PATCH entry for accepted issue #34.
- `C:\code\repo-template\plans\023.feedback.md`: deleted the empty reviewer feedback marker as requested.

Ran:
- PASS: `bash -lc '<AGENTS.md conflict-marker gate command>'`
- PASS: injected `<<<<<<< test-marker` into tracked `.gitignore`; gate failed as expected; original bytes restored.
- PASS: `printf '' | xargs -0 sh -c '<gate wrapper>' ...` for no-file handling.
- PASS: `git diff --check -- AGENTS.md CHANGELOG.md`
- PASS: `node -e "JSON.parse(require('fs').readFileSync('template-manifest.json','utf8'))"`
- PASS: manifest enforcement `node -e "<template-manifest tracked-file check>"`

Result:
- Clean tree gate exits 0.
- Marker in a tracked non-md/yml/json/jsonl file is caught.
- New tracked artifact types are covered automatically via `git ls-files`.

Open items: none.

# Stall outcome

Class: verify-failed
Detail: verify failed exit 1


# Stall outcome

Class: verify-failed
Detail: verify failed exit 1
