Changed:
- `AGENTS.md`: de-app-ified validation policy, added instantiated `## Verify gate`, marker tiers, manifest-based TEMPLATE-SELF gate, incident-log caveat.
- `README.md`, `TODO.md`, `SECURITY.md`, `.template-sync.json`, `.gitignore`, `CHANGELOG.md`, `TEMPLATE_VERSION`: setup audit/tiering, one-shot TODO semantics, sync sentinels, tree-lock ignore, v2.0.0 release.
- `.github/workflows/ci.yml`, `.github/ISSUE_TEMPLATE/task.md`, `.github/pull_request_template.md`: CI permissions/concurrency/YAML fix, issue label, prose PR prompts.
- `.ops/README.md`, `docs/INCIDENTS.md`, `docs/RUNBOOK.md`, `docs/ARCHITECTURE.md`, `docs/OBSERVABILITY.md`, ADRs `0000`-`0005`: placeholder unification, incident schema placement, deciders/evidence guidance, dependency posture.
- `template-manifest.json`, `plans/drafts/000-smoke.md`: new manifest and enrollment smoke plan.
- `plans/QUEUE.md`, plans `001`-`004`: canonical marker/conflict-grep cleanup so audits do not self-match.
- Deleted `plans/003.feedback.md`: reviewer sidecar addressed.

Ran:
- `bash -lc "cd /c/code/repo-template; if grep -rn '<''<<<<<<' --include='*.md' --include='*.yml' --include='*.json' .; then exit 1; else exit 0; fi"`
- `node -e "const m=require('C:/code/repo-template/template-manifest.json');const {execSync}=require('child_process');const tracked=execSync('git -C C:/code/repo-template ls-files',{encoding:'utf8'}).trim().split(/\r?\n/).filter(Boolean);const missing=tracked.filter(f=>!m[f]);if(missing.length){console.error('unmanifested:',missing);process.exit(1)}console.log('tracked manifest ok')"`
- `bash -lc "cd /c/code/repo-template; grep -q '2.0.0' TEMPLATE_VERSION"`
- `bash -lc "cd /c/code/repo-template; if grep -rn 'TODO(setup)' --include='*.md' . | grep -v 'TODO(setup)!*:' | grep -vE 'setup markers|grep'; then exit 1; else exit 0; fi"`
- `node -e "const fs=require('fs'); for (const f of ['C:/code/repo-template/.template-sync.json','C:/code/repo-template/template-manifest.json']) JSON.parse(fs.readFileSync(f,'utf8')); console.log('json ok')"`
- `node -e "const https=require('https'),fs=require('fs'),vm=require('vm'); https.get('https://cdn.jsdelivr.net/npm/js-yaml@4.1.0/dist/js-yaml.min.js',res=>{let d='';res.on('data',c=>d+=c);res.on('end',()=>{try{const ctx={}; vm.createContext(ctx); vm.runInContext(d,ctx); ctx.jsyaml.load(fs.readFileSync('C:/code/repo-template/.github/workflows/ci.yml','utf8')); console.log('yaml parse ok');}catch(e){console.error(e.message||e);process.exit(1)}});}).on('error',e=>{console.error(e);process.exit(1)});"`
- `node -e '...manifest including new files...'`
- `node -e '...fresh-clone setup audit simulation...'`
- `$count=(rg "TODO\(setup!\):" C:\code\repo-template | Measure-Object).Count; Write-Output "must-answer marker count $count"; if ($count -lt 10 -or $count -gt 14) { exit 1 }`
- `rg -n "## Verify gate|\{\{VERIFY_GATE_CMD\}\}" C:\code\repo-template\AGENTS.md`
- `rg -n "redeploys|/health|typecheck" C:\code\repo-template\AGENTS.md`
- `git -C C:\code\repo-template diff --check`
- `git -C C:\code\repo-template status --short --ignored`

Result:
- Pass: all final checks above.
- YAML initially caught a real syntax issue: `bad indentation of a mapping entry (21:31)`; fixed by quoting the `run:` scalar.
- Local YAML parser probes were unavailable (`ruby` not found, PyYAML missing, `js-yaml` CLI not recognized), so final YAML parse used CDN-loaded `js-yaml`.

Open items:
- No plan items left open. Ignored orchestrator sidecars remain: `.orchestrator-tree.lock`, `plans/003.log.md`, `plans/003.result.md`.