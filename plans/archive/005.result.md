Changed:
- `docs/MIGRATION.md` — added MAJOR rollout order with default canary `gmail-markdown` and green criteria.
- `AGENTS.md` — TEMPLATE-SELF versioning rule now points to canary-first rollout order.
- `CHANGELOG.md` — recorded the doctrine change under Unreleased/Changed.
- `TEMPLATE_VERSION` — bumped `2.2.0` → `2.3.0`.
- `template-manifest.json` — added missing tracked plan/archive entries so the repo’s manifest gate passes.

Ran:
- `& 'C:\Program Files\Git\bin\bash.exe' -lc 'cd /c/code/repo-template && grep -q "Rollout order" docs/MIGRATION.md && grep -qi "canary" docs/MIGRATION.md AGENTS.md'`
- `& 'C:\Program Files\Git\bin\bash.exe' -lc 'cd /c/code/repo-template && grep -qE "^[0-9]+\.[0-9]+\.[0-9]+$" TEMPLATE_VERSION'`
- `node -e "JSON.parse(require('fs').readFileSync('template-manifest.json','utf8'))"`
- `node -e 'const m=require("./template-manifest.json");const allowed=new Set(["copy","merge","self","generated"]);const {execSync}=require("child_process");const tracked=execSync("git ls-files",{encoding:"utf8"}).trim().split(/\r?\n/).filter(Boolean);const missing=tracked.filter(f=>!m[f]);const invalid=Object.entries(m).filter(([,v])=>!allowed.has(v)).map(([k,v])=>`${k}:${v}`);if(missing.length||invalid.length){if(missing.length)console.error("unmanifested:",missing);if(invalid.length)console.error("invalid manifest modes:",invalid);process.exit(1)}'`
- `if (Select-String -Path AGENTS.md,CHANGELOG.md,docs/MIGRATION.md,TEMPLATE_VERSION,template-manifest.json -Pattern '<<<<<<<' -Quiet) { exit 1 }; git diff --check -- AGENTS.md CHANGELOG.md docs/MIGRATION.md TEMPLATE_VERSION template-manifest.json; git status --short`

Result:
- Plan rollout grep: pass.
- Semver `TEMPLATE_VERSION`: pass.
- JSON parse: pass.
- Template manifest gate: pass.
- Conflict marker + diff whitespace check: pass.

Open items:
- Initial direct PowerShell `grep` attempt failed because `grep` is not on PATH; reran the same checks through Git Bash successfully.
- `adopt-project` skill was inspected for sync duty; it does not currently mention the new canary-first MAJOR rollout doctrine. I left it unchanged because it lives outside this repo and the plan scope was repo-template.