**Changed (files + why)**
- `model-boundary.json`: added to the Git index so it is part of the branch diff.
- `AGENTS.md`, `template-manifest.json`, `README.md`, `docs/ARCHITECTURE.md`, `docs/RUNBOOK.md`, `TODO.md`, `CHANGELOG.md`, `TEMPLATE_VERSION`: existing plan edits remain in place for model-boundary policy, adoption prompts, scanner remediation, manifest copy behavior, and 2.4.0 metadata.
- `C:\code\repo-template\plans\025.feedback.md`: deleted after addressing the reviewer request.

**Ran (exact commands)**
- `git add -- model-boundary.json`
- `Remove-Item -LiteralPath C:\code\repo-template\plans\025.feedback.md`
- `node -e "JSON.parse(require('fs').readFileSync('model-boundary.json','utf8')); JSON.parse(require('fs').readFileSync('template-manifest.json','utf8')); console.log('json ok')"`
- `Test-Path -LiteralPath C:\code\repo-template\plans\025.feedback.md`
- Updated template self-check `node -e "...boundaryErrors..."`
- `git diff --check`
- `git diff --cached --check`

**Result (pass/fail each)**
- `git add -- model-boundary.json`: pass; status now shows `A  model-boundary.json`.
- Feedback deletion: pass; `Test-Path` returned `False`.
- JSON parse check: pass.
- Template self-check: pass.
- `git diff --check`: pass.
- `git diff --cached --check`: pass.

**Open items**
- None.