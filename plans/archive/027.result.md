Changed
- Added `.user-surface-lint.json`, `.user-surface-lint.schema.json`, `scripts/lint-user-surface-leaks.mjs`, and `tests/fixtures/user-surface-lint/**` for the new deterministic user-surface leak lint.
- Updated `AGENTS.md`, `.github/workflows/ci.yml`, `README.md`, and `.github/pull_request_template.md` to wire/document the gate and add the user-surface review checklist.
- Updated `CHANGELOG.md`, `TEMPLATE_VERSION` to `2.5.0`, and `template-manifest.json` for the new template structure.

Ran
- `node --check scripts/lint-user-surface-leaks.mjs`
- `node scripts/lint-user-surface-leaks.mjs --self-test`
- `node scripts/lint-user-surface-leaks.mjs --config .user-surface-lint.json`
- `node -e "const fs=require('fs'); for (const f of ['template-manifest.json','.user-surface-lint.json','.user-surface-lint.schema.json']) JSON.parse(fs.readFileSync(f,'utf8'));"`
- YAML parse check over `*.yml` via Python/PyYAML
- Template self manifest/model-boundary gate Node command from `AGENTS.md`
- `node --test`
- Direct fixture assertions for bad, allowlisted, and empty configs
- `git diff --check`

Result
- Pass: script syntax, self-test, default no-op config, JSON parse, YAML parse, template self gate, fixture assertions, whitespace check.
- Pass: `node --test` ran successfully with 0 tests discovered.
- Bad fixture correctly exits nonzero and reports env-var, `restart the Docker stack`, and absolute-path findings.

Open items
- None.