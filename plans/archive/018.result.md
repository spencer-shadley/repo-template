Changed:
- `docs/MIGRATION.md` — added concrete MAJOR rollout canary rules: re-validation/update rule, smallest-verify-surface criterion, attribution rule based on the migration PR changed-file set, 3-drain/24h green window, delayed failure guard, and red failure path.
- `CHANGELOG.md` — updated `[Unreleased]` to describe the tightened canary rollout doctrine.

Ran:
- `rg -n "attributable to the migration|attributed|migration-attributed|canary|green|red|observation window|smallest verify surface" docs/MIGRATION.md CHANGELOG.md` — pass
- `node -e "JSON.parse(require('fs').readFileSync('template-manifest.json','utf8')); console.log('template-manifest.json ok')"` — pass
- `git diff -- docs/MIGRATION.md CHANGELOG.md` — pass
- Attempted plan verify gate via `bash -lc ...` — fail

Result:
- Pass: targeted acceptance scan
- Pass: JSON parse for `template-manifest.json`
- Fail: verify gate command invocation failed in PowerShell quoting before executing the bash snippet.

Exact error:
```text
ParserError: 
Line |
   2 |  … m=require('./template-manifest.json');const allowed=new Set(['copy',' …
     |                                                                ~
     | Missing type name after '['.
```

Open items:
- Re-run the plan verify gate with corrected PowerShell-safe quoting.