# Plan Critic Verdict a481abb6267fb0d1

- **Draft hash:** a481abb6267fb0d1
- **Plan:** 032
- **Verdict:** APPROVE
- **Blocking findings:** 0

```json
{
  "draftHash": "a481abb6267fb0d1",
  "planPath": ".scratch/repo-template-wave/repo-template-plan-disposition.md",
  "verdict": "APPROVE",
  "findings": [
    {
      "id": "pc-dcb8a50877b0",
      "category": "insufficient_verify_gate",
      "target": "Verify block / corepack pnpm validate & corepack pnpm test",
      "blocking": false,
      "evidence": "repo-template has no package.json/pnpm; AGENTS.md:11-15 self-gate is `lint-user-surface-leaks.mjs --self-test` + node manifest scan (Plan 029 uses it; Plan 027.critic flagged identical pnpm dead-weight). These lines presume 030 provisions root scripts and omit this repo's mandatory template-self gate.",
      "requiredEdit": "Replace `corepack pnpm validate`/`test` with the AGENTS.md template-self gate (`node scripts/lint-user-surface-leaks.mjs --self-test` plus the manifest/conflict-marker `node -e` scan), or gate the pnpm lines on 030-provisioned root scripts and add the self-gate alongside."
    },
    {
      "id": "pc-f3676ec3ef6c",
      "category": "insufficient_verify_gate",
      "target": "Verify block / node ledger check vs acceptance-criteria (queue bijection + archived bytes)",
      "blocking": false,
      "evidence": "Gate only checks 020-not-duplicated and 028/029 presence; it does not assert the 'Queue/live-plan bijection is exact' criterion (no archived plan in Pending, each retained auto plan once) nor 'archived artifacts retain original bytes and recorded hashes'.",
      "requiredEdit": "Add deterministic checks: assert no archived plan number appears in `## Pending`, each retained `ready for codex` auto plan appears exactly once, and recompute `git hash-object` of each moved artifact equals its ledger `originalBlobSha`/content hash."
    },
    {
      "id": "pc-61544b12bfdb",
      "category": "likely_failure_mode",
      "target": "Changes #1 evidence schema vs Verify `row.evidence.length`",
      "blocking": false,
      "evidence": "Verify throws when `!row.evidence.length` for non-salvage rows, requiring `evidence` be a length-bearing array/string; Changes #1 describes evidence as a bundle of fields (replacement plan/commit/blob/test), leaving shape unpinned. An object-shaped evidence would fail with valid data.",
      "requiredEdit": "Pin `evidence` in the schema as a non-empty array of evidence references so `row.evidence.length` is well-defined."
    },
    {
      "id": "pc-d1601a3451df",
      "category": "unclear_acceptance",
      "target": "Acceptance criteria / 'increases clarity and forward autonomy without creating a new human gate'",
      "blocking": false,
      "evidence": "Subjective criterion with no machine-checkable or evidence-bound signal; unverifiable at the gate.",
      "requiredEdit": "Operationalize as a concrete check (e.g., diff introduces no new Pending human-tier row and no new approval step) or move it to rationale rather than an acceptance checkbox."
    }
  ],
  "rounds": [
    {
      "round": 1,
      "verdict": "APPROVE",
      "findings": [
        {
          "id": "pc-dcb8a50877b0",
          "category": "insufficient_verify_gate",
          "target": "Verify block / corepack pnpm validate & corepack pnpm test",
          "blocking": false,
          "evidence": "repo-template has no package.json/pnpm; AGENTS.md:11-15 self-gate is `lint-user-surface-leaks.mjs --self-test` + node manifest scan (Plan 029 uses it; Plan 027.critic flagged identical pnpm dead-weight). These lines presume 030 provisions root scripts and omit this repo's mandatory template-self gate.",
          "requiredEdit": "Replace `corepack pnpm validate`/`test` with the AGENTS.md template-self gate (`node scripts/lint-user-surface-leaks.mjs --self-test` plus the manifest/conflict-marker `node -e` scan), or gate the pnpm lines on 030-provisioned root scripts and add the self-gate alongside."
        },
        {
          "id": "pc-f3676ec3ef6c",
          "category": "insufficient_verify_gate",
          "target": "Verify block / node ledger check vs acceptance-criteria (queue bijection + archived bytes)",
          "blocking": false,
          "evidence": "Gate only checks 020-not-duplicated and 028/029 presence; it does not assert the 'Queue/live-plan bijection is exact' criterion (no archived plan in Pending, each retained auto plan once) nor 'archived artifacts retain original bytes and recorded hashes'.",
          "requiredEdit": "Add deterministic checks: assert no archived plan number appears in `## Pending`, each retained `ready for codex` auto plan appears exactly once, and recompute `git hash-object` of each moved artifact equals its ledger `originalBlobSha`/content hash."
        },
        {
          "id": "pc-61544b12bfdb",
          "category": "likely_failure_mode",
          "target": "Changes #1 evidence schema vs Verify `row.evidence.length`",
          "blocking": false,
          "evidence": "Verify throws when `!row.evidence.length` for non-salvage rows, requiring `evidence` be a length-bearing array/string; Changes #1 describes evidence as a bundle of fields (replacement plan/commit/blob/test), leaving shape unpinned. An object-shaped evidence would fail with valid data.",
          "requiredEdit": "Pin `evidence` in the schema as a non-empty array of evidence references so `row.evidence.length` is well-defined."
        },
        {
          "id": "pc-d1601a3451df",
          "category": "unclear_acceptance",
          "target": "Acceptance criteria / 'increases clarity and forward autonomy without creating a new human gate'",
          "blocking": false,
          "evidence": "Subjective criterion with no machine-checkable or evidence-bound signal; unverifiable at the gate.",
          "requiredEdit": "Operationalize as a concrete check (e.g., diff introduces no new Pending human-tier row and no new approval step) or move it to rationale rather than an acceptance checkbox."
        }
      ],
      "confidence": "high"
    }
  ],
  "trigger": {
    "needed": true,
    "priority": 2,
    "priorityLabel": "P2",
    "riskPromoted": false,
    "capabilityTier": "advanced",
    "reasons": [
      "priority:P2"
    ],
    "declaredGates": [],
    "protectedPathHits": []
  },
  "blocking": false,
  "plan": "032",
  "planFile": "plans/032-reconcile-repo-template-plan-debt-without-discarding-commiss.md"
}
```
