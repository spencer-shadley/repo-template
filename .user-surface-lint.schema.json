{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "User surface leak lint config",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "include"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "userSurface": {
      "enum": ["none"],
      "description": "Explicit opt-out for repositories with no user-facing surface. Set to \"none\" only when \"include\" is intentionally empty; the guard rejects an empty \"include\" without this declaration."
    },
    "include": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Globs for files containing user-visible strings or response messages. Must be non-empty unless userSurface is explicitly \"none\"."
    },
    "allowlist": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "path",
          "justification"
        ],
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "File path or glob relative to the repository root."
          },
          "line": {
            "type": "integer",
            "minimum": 1
          },
          "rule": {
            "enum": [
              "env-var",
              "infra-noun",
              "absolute-path",
              "internal-error"
            ]
          },
          "match": {
            "type": "string",
            "minLength": 1
          },
          "justification": {
            "type": "string",
            "minLength": 1
          }
        }
      },
      "default": []
    }
  }
}
